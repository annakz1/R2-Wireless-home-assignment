server.port=4000
spring.application.name=be-high-level

auth.correct-password=r2isthebest

# Springdoc / Swagger UI (so /swagger-ui.html and /v3/api-docs resolve)
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Logging (optional overrides; main config in logback-spring.xml)
# logging.level.root=INFO
# logging.level.com.r2=DEBUG
# logging.file.name=logs/be-high-level.log
